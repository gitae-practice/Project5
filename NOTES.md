# Project5 진행 노트

## 완료된 작업

### 2026-06-25 (계속)
- 장소 검색 모달에 과거 방문 미리보기 추가 — 검색 결과에 "N번 방문 · ★평점" 표시
  - 백엔드: `findRatingStatsByName` 쿼리에서 `WHERE rating IS NOT NULL` 제거하고 전체 방문 횟수(`visitCount`)도 함께 반환
- 장소 재선택 시 직전 별점 자동 채움 기능 제거 — 전역 평균 도입 이후로는 새 방문에 옛 별점이 자동으로 채워지면 오히려 혼란을 줘서 되돌림 (`findLastRating` 유틸 삭제)
- 백엔드 단위 테스트 3개 추가 (`ContactServiceTest` — 동행인 조회, 장소 별점 통계 매핑)
- 프론트엔드 Vitest 도입 + 날짜 유틸(`toDateKey`) 테스트 2개 추가, `npm audit fix`로 기존 취약점 정리

### 2026-06-25
- 장소 별점 평균을 지인별 집계에서 전역 집계로 변경 — 같은 장소를 다른 지인과 가도 평균에 그동안의 모든 방문이 반영됨 (방문은 독립적으로 별점 가능, 평균만 전역)
  - 백엔드: `MeetingPlaceRepository.findRatingStatsByName()`, `GET /api/contacts/meeting-places/ratings` 신규
  - 커밋: d38bb71
- 지도 강조 마커 z-index 버그 수정 — 같은 좌표에 마커가 겹칠 때 강조(주황) 핀이 기본 핀에 가려지던 문제
  - 커밋: bde93a4

### 2026-06-24
- 히트맵 클릭 시 달력 위치로 자동 스크롤
- 지도 인포윈도우에서 여러 번 방문한 장소는 "방문 기록 보기" 클릭 시 날짜별 동행인 팝업
  - 백엔드: 같은 groupId로 묶인 다른 지인 이름 조회하는 `GET /api/contacts/meetings/{id}/companions` 신규
  - 커밋: 2f4f55b

### 2026-06-23
- 만남 장소에 별점(방문 평가) + 같은 장소 N회 방문 표시 추가 — `meeting_places.rating` 컬럼, `StarRating` 컴포넌트
- 장소 재선택 시 직전 별점 자동 채움, 지도 별점을 평균값(0.5 단위 반개 지원)으로 표시
- Tailwind 임의값/구식 클래스 정리 (canonical class 경고 해결)
- 커밋: ac622d8, 23dcd4b, 61a1893, b121a3d

### 2026-06-22
- 히트맵 클릭 시 달력 자동 이동 + 선택 기간 지도 핀 강조(오렌지 커스텀 마커) 기능 추가
- 만남 지도/히트맵 버그 수정: 날짜 타임존 밀림(`toISOString()` → 로컬 날짜 유틸), 정보창 안 닫힘, 말풍선 줄바꿈, 핀 모양
- 커밋: a8f3dbd, a08e08e

### 2026-06-20
- Tailwind CSS 전체 적용 완료 (설치만 되고 미사용이던 상태 해결)
- 기술 스택 문서 정정 + 미사용 의존성(lucide-react, react-kakao-maps-sdk, @types/react-router-dom, spring-boot-starter-validation) 제거
- README에 스크린샷 추가하고 포트폴리오용으로 재작성
- 기존 lint 에러 16개(13 에러 + 3 경고) 전부 정리, 히트맵 Y축 눈금 중복 키 경고 수정
- 커밋: 822f6fc, 19e2b1a, 43002f1, a304871, 730217a, aac01ff, 2bb873b

### 2026-06-18
- Pretendard 폰트 적용 (CDN 로드 후 전체 화면 일괄 반영)

### 2026-06-17
- 미분류 섹션 드롭존 버그 수정 — 드래그 중에만 표시, 그룹 밖으로 빼낼 때 드롭 불가 문제 해결

### 2026-06-16
- 디자인 개선 — 프로필 헤더 관계별 그라디언트, 글라스 통계 바, 취향/선물 카드, 사이드바 라이트 슬레이트(#f8fafc)
- Prettier 도입, 전체 코드 스타일 통일

### 2026-06-12 ~ 2026-06-15
- 만남 히트맵 추가 — 지인 상세 만남 탭 상단, 최근 52주 에리어 차트(베지어 곡선 + 오렌지 그라디언트), 월 구분선, 호버 툴팁
- 히트맵 기간 필터(년/월/주) + 클릭 시 만남 목록 필터 연동

### 2026-06-10
- 그룹 중복 이름 방지 (백엔드 400 응답 + 프론트 인라인 에러 표시)
- 지인 추가 폼에 그룹 선택 드롭다운 추가 (그룹 없으면 미표시)
- 그룹 순서 드래그 안정화 (DOM 실시간 재정렬 제거 → 드롭 시에만 순서 변경)
- 커밋: 0312cda

- 커스텀 그룹 순서 드래그앤드롭 재정렬 추가
  - 사이드바 그룹 헤더를 드래그해서 순서 변경
  - 드래그 중 파란 상단 보더로 드롭 위치 시각적 표시
  - 낙관적 업데이트 후 PATCH /api/groups/order 로 sortOrder 일괄 저장
  - 커밋: 7a55956

### 2026-06-09
- 홈 대시보드 "예정된 만남" 섹션 추가
  - 오늘 이후(오늘 포함) 날짜로 등록된 만남을 홈 상단에 표시
  - D-Day / D-N 배지 (오늘이면 파란 배지, 이후 날짜면 회색)
  - groupId 기반 그룹핑 적용 (여러 명 동시 만남도 한 카드로)
  - 백엔드: MeetingRepository.findUpcomingMeetings(), DashboardService, DashboardDto 업데이트
  - 커밋: 19ad79a

### 2026-06-08
- 그룹 만남 groupId(UUID) 도입: 영구적인 그룹 링크
  - 같은 그룹으로 만든 Meeting 레코드들이 groupId를 공유
  - 개별 수정 후에도 그룹 유지, 기존 그룹에 지인 추가 가능
- 홈 대시보드 만남 수정 기능 개선
  - 일괄/개별 수정 모드 드롭다운 추가
  - 개별 수정: 멤버별 메모 따로 입력
  - 메모가 하나라도 다르면 수정 폼 열 때 개별 수정 모드로 자동 전환
  - 그룹 멤버 제거 기능 (마지막 1명은 제거 불가)
  - 개별 수정 모드에서 멤버 제거 시 해당 메모 입력란도 즉시 사라짐
- 대시보드 백엔드 limit 5→30으로 변경 (그룹핑 후 5그룹 표시)
- 레거시 만남(groupId 없음) 날짜+장소 기반 fallback 그룹핑

## 기술 스택
- Frontend: React + TypeScript + Vite
- Backend: Spring Boot 4.x + JPA/Hibernate + MySQL 8
- 인프라: 로컬 개발 환경 (localhost:5173 / :8080)
